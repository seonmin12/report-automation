pandas>=2.0
numpy>=1.24
openpyxl>=3.1
matplotlib>=3.7
pytest>=7.4

# 2단계 웹 서비스화 (FastAPI JSON API + Vue 프론트엔드, Vercel 배포)
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9  # 파일 업로드(form/multipart) 처리
httpx>=0.27  # FastAPI TestClient용 (테스트 전용)
