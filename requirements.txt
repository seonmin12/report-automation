pandas>=2.0
numpy>=1.24
openpyxl>=3.1
matplotlib>=3.7
pytest>=7.4

# 2단계 웹 서비스화 (FastAPI + Vercel, 조회/다운로드 전용 대시보드)
fastapi>=0.110
uvicorn>=0.29
jinja2>=3.1
httpx>=0.27  # FastAPI TestClient용 (테스트 전용)
